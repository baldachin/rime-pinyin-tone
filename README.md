# 📝 Rime 带调拼音输入方案（pinyin_tone）

> 用 `bei3 jing1` 输入 → 自动输出标准 Unicode 带调拼音：**běi jīng**

这是一个基于 [Rime](https://rime.im/)（中州韵 / 小狼毫 / 同文等）的输入法方案，专为**语言学、汉语教学、语音标注、学术写作**等场景设计。  
无需输入汉字，直接输出符合国际标准的带声调拉丁拼音（如 `ā á ǎ à`, `nǚ`, `lüè`）。

---

## ✨ 特点

- ✅ **数字标调**：输入 `nv3` → 输出 `nǚ`；`hao4` → `hào`
- ✅ **完整覆盖**：支持全部普通话音节 + 5 声调（含轻声）
- ✅ **标准 Unicode**：使用组合字符（Combining Diacritics），兼容所有现代系统
- ✅ **一键上屏**：唯一候选自动确认，高效流畅
- ✅ **轻量纯净**：无用户词典干扰，输出结果标准化
- ✅ **与 IPA 方案同源**：延续 `ipa_yunlong` 的简洁设计哲学

---

## 🚀 使用示例

| 用户输入       | Rime 输出     |
|----------------|---------------|
| `zhong1`       | `zhōng`       |
| `guo2`         | `guó`         |
| `nv3`          | `nǚ`          |
| `lüe4`（输入 `lve4`）| `lüè`    |
| `bei3 jing1`   | `běi jīng`    |

> 💡 输入时用 `v` 代替 `ü`（如 `nv`, `lve`），这是中文输入惯例。

---

## 📦 安装方法

### 1. 下载本方案文件
将以下两个文件放入你的 Rime 用户目录（通常为 `%APPDATA%\Rime` 或 `~/Library/Rime` 或 `~/.config/ibus/rime`）：

- [`pinyin_tone.schema.yaml`](./pinyin_tone.schema.yaml)
- [`pinyin_tone.dict.yaml`](./pinyin_tone.dict.yaml)

> 🔗 若尚未生成词典，可运行 [`generate_pinyin_tone.py`](./generate_pinyin_tone.py) 自动生成（需 Python + pypinyin）。

### 2. 修改 `default.custom.yaml`
在你的 Rime 配置中启用该方案：

```yaml
patch:
  schema_list:
    - schema: pinyin_tone   # ← 添加这一行
    # - schema: luna_pinyin
